---
interface Props {
  description: string;
}

const { description: rawDescription } = Astro.props;
const paragraphList = rawDescription.split("\n\n");
---

<div class="flex flex-col gap-y-3">
  {paragraphList.map((para) => <p>{para}</p>)}
</div>
